[package]
name = "common-types"
version = "0.1.0"
edition = "2024"

[dependencies]
bt-hci = { version = "0.2.1", optional = true }
heapless = { version = "0.8.0", features = ["serde"] }
postcard = "1.1.3"
serde = { version = "1.0.228", default-features = false, features = ["derive"] }

[features]

from-impl = ["dep:bt-hci"]
