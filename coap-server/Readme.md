# Proof of concept RD + logic in one server

```sh
pipx run server.py
```

## Generate a key

```sh
pipx run generate.py
```

The command will output a public key, put this key for `kccs` in `../gateway/peers.yml`.
